<%= form_with(model: ituser, local: true) do |form| %>
  <% if ituser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ituser.errors.count, "error") %> prohibited this ituser from being saved:</h2>

      <ul>
      <% ituser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
