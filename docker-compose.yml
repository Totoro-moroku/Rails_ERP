version: '3'

services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: root
    ports:
      - "3306:3306"

  backend:
    build: ./backend
    command: /bin/sh -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - ./backend:/app_back
    ports:
      - "3000:3000"
    links:
      - db

  frontend:
    build: ./frontend
    command: sh -c "cd app && npm start"
    volumes:
      - ./frontend:/usr/src/app_frontend
      - /usr/src/app_frontend/app/node_modules
    ports:
      - 8000:8000
    links:
      - backend